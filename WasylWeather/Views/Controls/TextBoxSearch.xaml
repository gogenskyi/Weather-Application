<UserControl x:Class="WasylWeather.Views.Controls.TextBoxSearch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WasylWeather.Views.Controls" xmlns:viewmodels="clr-namespace:WasylWeather.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300">
    <Grid>
        <Border Background="#FF656565" Opacity="0.7" BorderThickness="0" Margin="0" CornerRadius="18">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="1" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                <TextBox Margin="10" Padding="10" Background="Transparent" Foreground="White" BorderThickness="0" Text="{Binding Text}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
