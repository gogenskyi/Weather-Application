<UserControl x:Class="WasylWeather.Views.Controls.TextBoxSearch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WasylWeather.Views.Controls" xmlns:viewmodels="clr-namespace:WasylWeather.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="300">

    <Border Background="#FF656565" Opacity="0.8" BorderThickness="0" Margin="0" CornerRadius="18" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="1" Opacity="1"/>
        </Border.Effect>
        <Grid Background="Transparent">
            <TextBox x:Name="Textbox" Margin="3" VerticalAlignment="Center" Background="Transparent" Foreground="White" BorderThickness="0" TextChanged="Textbox_TextChanged"/>
            <Label x:Name="EnterTXT" Content="Enter Your Location..." FontSize="14" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="WhiteSmoke" Panel.ZIndex="-1"/>
            <Button HorizontalAlignment="Right" Click="Button_Click" FontSize="15" Opacity="1" Height="15" Width="15" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Margin="2 2 5 2">
                <Button.Content>
                    <!--<Image Source="/Views/Images/Close.Button.png"/>-->
                    <TextBlock Text="X" Foreground="White" FontFamily="Ebrima" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button.Content>
            </Button>
        </Grid>
    </Border>
</UserControl>
